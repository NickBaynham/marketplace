export const meta = {
  title: "Welcome",
  date: "{{year}}-01-01",
  summary: "First post. Replace this with your own."
};

# Welcome

This is the first blog post for {{site_name}}. Edit `content/blog/welcome.mdx` to change it,
or add a new `.mdx` file in `content/blog/` to publish another post.

Each post is an MDX file with a `meta` export (title, date, summary) and a default body.
